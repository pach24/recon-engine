server:
  port: 8082

spring:
  application:
    name: reconciliation-engine

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
